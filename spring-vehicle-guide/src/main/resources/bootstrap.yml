server:
  port: 5000

spring:
  application:
    name: "vehicle-guide"

  datasource:
    hikari:
      driver-class-name: com.mysql.jdbc.Driver
      username: "root"
      password: ""
    url: "jdbc:mysql://localhost:3306/vehicle-guide"

  jpa:
    show-sql: true
    hibernate:
      use-new-id-generator-mappings: true
      ddl-auto: update
